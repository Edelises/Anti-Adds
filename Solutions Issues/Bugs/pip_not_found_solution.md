# Solution: Use `python3 -m pip`

## Description
Instead of calling `pip` directly, use the Python module flag to ensure the correct pip instance associated with `python3` is used.

## Implementation
```bash
python3 -m pip install opencv-python pyautogui numpy pillow pytesseract
```

## Result
Dependencies installed successfully.
